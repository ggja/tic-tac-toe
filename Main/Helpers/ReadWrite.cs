using MathNet.Numerics.Data.Text;
using MathNetExample.Population;

namespace MathNetExample;

public static class ReadWrite
{
    
    // public static void SaveMatrixToFile(Player matrix, string folderPath = "/Users/Shared/test")
    // {
    //     folderPath = Path.Combine(folderPath, DateTime.Now.ToString("yyyy-MM-dd"));
    //     using var writer = new StreamWriter(folderPath);
    //     DelimitedWriter.Write(writer, matrix, ",");
    // }
    //
    // public static Player ReadMatrixFromFile(string filePath)
    // {
    //     using var reader = new StreamReader(filePath);
    //     return DelimitedReader.Read<double>(reader, false, ",", false);
    // }
}